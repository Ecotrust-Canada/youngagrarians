.detail-page.standalone
  %h4{ style: "margin-bottom:30px"} Request Password Reset
  = form_for( Struct.new( :email ).new, as: 'password_reset', url: password_resets_path, builder: FormBuilder,  method: :post ) do |f|
    = f.email_field :email
    .btn-wrap
      %button.btn.btn-primary.btn-small{ name: 'step', value: 'description' } Reset Password
