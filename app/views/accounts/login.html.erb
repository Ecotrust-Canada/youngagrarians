
<div class='detail-page standalone'>
<div class="container-fluid" id="locations-admin-list">
	<div class="container">
		<div class="page-header">
			<h1>Administration Login</h1>
		</div>

		<% if params[:return_url] %>
			<%= render 'login', :return_url => params[:return_url] %>
		<% else %>
			<%= render 'login' %>
		<% end %>
	</div>
</div>
</div>
